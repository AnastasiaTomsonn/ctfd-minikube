replicaCount: 1

ingress:
  enabled: true
  className: "nginx"
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: minikube-ca-issuer
  hosts:
    - host: ctfd.minikube.internal
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - hosts:
        - ctfd.minikube.internal
      secretName: ctfd-tls

envVariables:
  - name: DATABASE_URL
    value: mysql+pymysql://ctfd_user:QZNDmTwMem4X87kl03LQ@mariadb-galera:3306/ctfd_db
  - name: REDIS_URL
    value: redis://default:nwfZ428mHqT5eBnGl4zA@redis-headless:6379
